[project]
name = "foxtrai-mcp"
version = "0.1.0"
description = "MCP server for Foxtrai AI painting platform"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=2.0.0",
    "httpx>=0.28.0",
    "Pillow>=10.0.0",
]

[project.scripts]
foxtrai-mcp = "foxtrai_mcp.server:main"
